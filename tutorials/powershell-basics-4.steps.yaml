- expectedCommand: Get-Command *process*
  hint: Use wildcards to search within command names.
  instruction: Use `Get-Command *process*` to find commands that work with processes.
  title: Search for Process Commands
  validateOutputIncludes:
  - Get-Process
- expectedCommand: Get-Process | Get-Member
  hint: Pipe Get-Process into Get-Member.
  instruction: Run `Get-Process | Get-Member` to inspect its properties and methods.
  title: Inspect Command Output with Get-Member
  validateOutputIncludes:
  - Method
  - Property
