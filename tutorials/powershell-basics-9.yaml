id: powershell-basics-9
title: Loops & Iteration
description: 'Master loops: for, foreach, while, and more.'
xp: 150
difficulty: Beginner
steps:
- id: foreach-loop
  title: Use a Loop
  instruction: 'Loop through items: `foreach ($x in 1..3) { Write-Host $x }`'
  command: foreach ($x in 1..3) { Write-Host $x }
  hint: Try looping from 1 to 3.
  validation:
    type: contains
    value: '3'
