- expectedCommand: New-Item hello.ps1
  hint: Use New-Item with the script name.
  instruction: Use `New-Item hello.ps1` to create a script file.
  title: Create a Script File
- expectedCommand: ./hello.ps1
  hint: Edit the file and run it using `./` prefix.
  instruction: Add `Write-Host 'Hello from script!'` inside the file and run it with
    `./hello.ps1`.
  title: Edit and Run the Script
  validateOutputIncludes:
  - Hello from script!
